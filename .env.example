# Optional Hugging Face token. Public Qwen models work without it.
HF_TOKEN=

# Persistent model cache outside the repository. Relative paths are resolved by Docker Compose.
SEEDMARK_MODEL_CACHE=../seedmark-model-cache
